<div class="container p-4">
    <div class="row">
        <div class="col-md-8">
            <div class="card-body">
                <table class="table table-striped">
                    <h1>Usuarios</h1> <a href="/suma/suma" class="btn btn-info">ir a suma</a>
                     <a href="/suma/motoadd" class="btn btn-info">ir a agregar motos</a>
                     <a href="/suma/motos" class="btn btn-info">ir a motos</a>
                    <thead>
                        <tr>
                            <th scope="col">Nª</th>
                            <th scope="col">marca</th>
                            <th scope="col">linea</th>
                            <th scope="col">cilindraje</th>
                            <th scope="col">matricula</th>
                            <th scope="col">fechadematricula</th>
                            <th scope="col">placa</th>
                            <th scope="col"></th>
                            <th scope="col"></th>

                        </tr>
                    </thead>
                    <tbody>
                        
                        <tr>
                            {{#each motos}}
                            <th scope="row"></th>
                            <td>{{id}}</td>
                            <td>{{marca}}</td>
                            <td>{{linea}}</td>
                            <td>{{cilindraje}}</td>
                            <td>{{matricula}}</td>
                            <td>{{fechamatricula}}</td>
                            <td>{{placa}}</td>
                            <td><a href="modifica/{{id}}" class="btn btn-info">modificar</a></td>
                            <td><a href="eliminar/{{id}}" class="btn btn-danger">eliminar</a></td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>